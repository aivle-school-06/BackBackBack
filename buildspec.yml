version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto21
  pre_build:
    commands:
      - chmod +x gradlew
      - ./gradlew --version
      - chmod +x scripts/*.sh
  build:
    commands:
      - ./gradlew clean test --stacktrace --info
  post_build:
    commands:
      - ./gradlew bootJar -x test

artifacts:
  files:
    - appspec.yml
    - build/libs/*.jar
    - scripts/**/*
